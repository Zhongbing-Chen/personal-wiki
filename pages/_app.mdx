import './style.css'
import { useRouter } from '../customRouter';
export default function App({ Component, pageProps }) {

  // Use the custom router instead of the default Next.js router
  return <Component {...pageProps} />;
}
